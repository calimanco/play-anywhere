#!/usr/bin/env node

const { version, name, main } = require('../package.json')
const { join } = require('path')
process.title = name
process.env.version = version

require(join(__dirname, '..', main))()
